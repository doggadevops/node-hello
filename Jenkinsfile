pipeline {
    agent any 

    stages {

        stage("Build Node App"){

            steps {

                script {
                    sh "npm install"
                }

            }

        }

    }
}
